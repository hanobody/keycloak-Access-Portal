apiVersion: apps/v1
kind: Deployment
metadata:
  name: access-portal
  namespace: keycloak
  labels:
    app: access-portal
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: access-portal
  template:
    metadata:
      labels:
        app: access-portal
    spec:
      containers:
        - name: web
          image: doctor8181/access-portal:ui-v3-c0043a9
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - secretRef:
                name: access-portal-secret
          env:
            - name: PORTAL_APPS_CONFIG_PATH
              value: /app/config/apps.runtime.json
          volumeMounts:
            - name: apps-config
              mountPath: /app/config/apps.runtime.json
              subPath: apps.runtime.json
          readinessProbe:
            httpGet:
              path: /signin
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /signin
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: apps-config
          configMap:
            name: access-portal-apps
